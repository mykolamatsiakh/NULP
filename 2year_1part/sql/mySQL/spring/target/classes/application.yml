spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_spring_vanyok?useSSL=false
    username: root
    password: root
    vlasnyk-class-name: com.mysql.jdbc.Driver
  jpa:
    # Show or not log for each sql query
    show-sql: true
    hibernate:
      # Hibernate ddl auto (validate, create, create-drop, update)
      ddl-auto: validate
      # Naming strategy
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy

server:
  port: 8080

